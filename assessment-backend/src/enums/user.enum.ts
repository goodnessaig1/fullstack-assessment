export enum UserRole {
  PRINCIPAL = "principal",
  TEACHER = "teacher",
  STAFF = "staff",
  STUDENT = "student",
}

export enum UserStatus {
  ACTIVE = "active",
  INACTIVE = "inactive",
}

export enum UserTitle {
  MR = "mr",
  MRS = "mrs",
  MISS = "miss",
  MS = "ms",
}

export enum UserGender {
  MALE = "male",
  FEMALE = "female",
}
